# Block all direct HTTP access to uploaded verification documents.
# Files are served only through PHP with proper authorization checks.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
